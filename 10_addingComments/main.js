// Author name : Noor Fatima
import chalk from "chalk"; // to make our cmd colorful
let radius = 5;
const pi = Math.PI;
let area = pi * radius ** 2; // Formula to calculate area
// This shows the area of a circle.
console.log(chalk.yellow(`The area of the circle with radius ${radius} is ${area}.`));
